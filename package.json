{
	"name": "jacly-monorepo",
	"private": true,
	"type": "module",
	"scripts": {
		"prepare": "husky",
		"build": "pnpm -r build",
		"build:watch": "pnpm -r --parallel build:watch",
		"rebuild": "pnpm clean && pnpm i && pnpm build",
		"test": "pnpm -r test",
		"lint": "pnpm -r lint",
		"lint:fix": "pnpm -r lint --fix",
		"format": "pnpm -r format",
		"format:check": "pnpm -r format:check",
		"type-check": "pnpm -r build && pnpm -r run type-check",
		"fix-all": "pnpm -r lint:fix && pnpm -r format",
		"check-all": "pnpm run format:check && pnpm run lint && pnpm run build",
		"clean": "pnpm -r clean",
		"clean-node" : "pnpm -r clean-node && rm -rf node_modules",
		"deps:check": "ncu --deep",
		"deps:update": "ncu -u --deep && pnpm install",
		"deps:update-interactive": "ncu -i --deep"
	},
	"devDependencies": {
		"@commitlint/cli": "^20.1.0",
		"@commitlint/config-conventional": "^20.0.0",
		"@commitlint/types": "^20.0.0",
		"husky": "^9.1.7",
		"npm-check-updates": "^19.1.2",
		"prettier": "^3.6.2"
	}
}
